import assert from 'assert'
import Rx from 'rx'
import {youMagineDriver} from './youMagineDriver'

//TODO: implement
describe('youMagineDriver', () => {

    /*it('should return incoming requests as stream', (done) => {
      const outgoing$  = Rx.Observable.just(undefined)
      const ymDriver = youMagineDriver(outgoing$)

      const input$ = ymDriver
        .forEach(message => {
          console.log("input message",message)
        })

    })

    it('should save outgoing data through the postMessage api', (done) => {

      const outgoing$  = Rx.Observable.just({output:42,somethingElse:"cool"})
      const inputs$    = youMagineDriver(outgoing$)
    })*/

})
